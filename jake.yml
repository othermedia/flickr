---
source_directory:     src
build_directory:      .
layout:               together
header:               header
builds:
  min:
    shrink_vars:      true
    private:          true

packages:

  flickr:
    files:
      - flickr
      - photo
      - jsonp
    meta:
      provides:
        - Flickr
        - Flickr.Client
        - Flickr.JSONP
      requires:
        - JS.Class

